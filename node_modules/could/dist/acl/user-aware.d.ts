import { Aware } from "../interfaces/acl";
import { Storage } from "../interfaces/driver";
export declare class UserAware implements Aware {
    storage: Storage;
    name: string;
    constructor(storage: Storage, name: string);
    can(action: string, resource: string): Promise<boolean>;
    cannot(action: string, resource: string): Promise<boolean>;
}
