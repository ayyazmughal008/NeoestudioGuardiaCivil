import { Aware } from "../interfaces/acl";
import { Storage } from "../interfaces/driver";
export declare class Acl {
    storage: Storage;
    constructor(storage: Storage);
    user(name: string): Aware;
    role(name: string): Aware;
}
